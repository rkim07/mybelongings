// This is a manager for external notifications that the MKPL Server is subscribing
import * as config from 'config';
import { Container, Inject } from 'typedi';

export class NotificationManager {

    constructor() {
    }
}
